@(Html.Kendo().Grid<CardioSence.Models.OtherDisease>()
              .Name("OtherDisease")
              .Columns(columns =>
              {
                  columns.Bound(product => product.OtherDiseaseId).Width(70);
                  columns.Bound(product => product.OtherDiseaseName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.OtherDiseaseId);
                        model.Field(product => product.OtherDiseaseId).Editable(false);
                    })
                    .Create(create => create.Action("OtherDiseaseCreate", "Catalog"))
                    .Read(read => read.Action("OtherDiseaseRead", "Catalog"))
                    .Update(update => update.Action("OtherDiseaseUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("OtherDiseaseDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

