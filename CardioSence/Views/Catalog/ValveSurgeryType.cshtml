@(Html.Kendo().Grid<CardioSence.Models.ValveSurgeryType>()
              .Name("ValveSurgeryType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ValveSurgeryTypeId).Width(70);
                  columns.Bound(product => product.ValveSurgeryTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ValveSurgeryTypeId);
                        model.Field(product => product.ValveSurgeryTypeId).Editable(false);
                    })
                    .Create(create => create.Action("ValveSurgeryTypeCreate", "Catalog"))
                    .Read(read => read.Action("ValveSurgeryTypeRead", "Catalog"))
                    .Update(update => update.Action("ValveSurgeryTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ValveSurgeryTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

