@(Html.Kendo().Grid<CardioSence.Models.VH>()
              .Name("VH")
              .Columns(columns =>
              {
                  columns.Bound(product => product.VHId).Width(70);
                  columns.Bound(product => product.VHName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.VHId);
                        model.Field(product => product.VHId).Editable(false);
                    })
                    .Create(create => create.Action("VHCreate", "Catalog"))
                    .Read(read => read.Action("VHRead", "Catalog"))
                    .Update(update => update.Action("VHUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("VHDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

