@(Html.Kendo().Grid<CardioSence.Models.DrugFrequency>()
              .Name("DrugFrequency")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DrugFrequencyId).Width(70);
                  columns.Bound(product => product.DrugFrequencyName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DrugFrequencyId);
                        model.Field(product => product.DrugFrequencyId).Editable(false);
                    })
                    .Create(create => create.Action("DrugFrequencyCreate", "Catalog"))
                    .Read(read => read.Action("DrugFrequencyRead", "Catalog"))
                    .Update(update => update.Action("DrugFrequencyUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DrugFrequencyDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

