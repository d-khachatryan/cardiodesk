@(Html.Kendo().Grid<CardioSence.Models.ArteriaStatus>()
              .Name("ArteriaStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ArteriaStatusId).Width(70);
                  columns.Bound(product => product.ArteriaStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ArteriaStatusId);
                        model.Field(product => product.ArteriaStatusId).Editable(false);
                    })
                    .Create(create => create.Action("ArteriaStatusCreate", "Catalog"))
                    .Read(read => read.Action("ArteriaStatusRead", "Catalog"))
                    .Update(update => update.Action("ArteriaStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ArteriaStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

