@(Html.Kendo().Grid<CardioSence.Models.HBSStatus>()
              .Name("HBSStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.HBSStatusId).Width(70);
                  columns.Bound(product => product.HBSStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.HBSStatusId);
                        model.Field(product => product.HBSStatusId).Editable(false);
                    })
                    .Create(create => create.Action("HBSStatusCreate", "Catalog"))
                    .Read(read => read.Action("HBSStatusRead", "Catalog"))
                    .Update(update => update.Action("HBSStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("HBSStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

