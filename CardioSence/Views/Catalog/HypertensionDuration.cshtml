@(Html.Kendo().Grid<CardioSence.Models.HypertensionDuration>()
              .Name("HypertensionDuration")
              .Columns(columns =>
              {
                  columns.Bound(product => product.HypertensionDurationId).Width(70);
                  columns.Bound(product => product.HypertensionDurationName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.HypertensionDurationId);
                        model.Field(product => product.HypertensionDurationId).Editable(false);
                    })
                    .Create(create => create.Action("HypertensionDurationCreate", "Catalog"))
                    .Read(read => read.Action("HypertensionDurationRead", "Catalog"))
                    .Update(update => update.Action("HypertensionDurationUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("HypertensionDurationDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

