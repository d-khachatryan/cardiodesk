@(Html.Kendo().Grid<CardioSence.Models.PVDT>()
              .Name("PVDT")
              .Columns(columns =>
              {
                  columns.Bound(product => product.PVDTId).Width(70);
                  columns.Bound(product => product.PVDTName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.PVDTId);
                        model.Field(product => product.PVDTId).Editable(false);
                    })
                    .Create(create => create.Action("PVDTCreate", "Catalog"))
                    .Read(read => read.Action("PVDTRead", "Catalog"))
                    .Update(update => update.Action("PVDTUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("PVDTDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

