@(Html.Kendo().Grid<CardioSence.Models.HIVStatus>()
              .Name("HIVStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.HIVStatusId).Width(70);
                  columns.Bound(product => product.HIVStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.HIVStatusId);
                        model.Field(product => product.HIVStatusId).Editable(false);
                    })
                    .Create(create => create.Action("HIVStatusCreate", "Catalog"))
                    .Read(read => read.Action("HIVStatusRead", "Catalog"))
                    .Update(update => update.Action("HIVStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("HIVStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

