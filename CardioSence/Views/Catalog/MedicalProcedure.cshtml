@(Html.Kendo().Grid<CardioSence.Models.MedicalProcedure>()
              .Name("MedicalProcedure")
              .Columns(columns =>
              {
                  columns.Bound(product => product.MedicalProcedureId).Width(70);
                  columns.Bound(product => product.MedicalProcedureName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.MedicalProcedureId);
                        model.Field(product => product.MedicalProcedureId).Editable(false);
                    })
                    .Create(create => create.Action("MedicalProcedureCreate", "Catalog"))
                    .Read(read => read.Action("MedicalProcedureRead", "Catalog"))
                    .Update(update => update.Action("MedicalProcedureUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("MedicalProcedureDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

