@(Html.Kendo().Grid<CardioSence.Models.HeartSoundType>()
              .Name("HeartSoundType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.HeartSoundTypeId).Width(70);
                  columns.Bound(product => product.HeartSoundTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.HeartSoundTypeId);
                        model.Field(product => product.HeartSoundTypeId).Editable(false);
                    })
                    .Create(create => create.Action("HeartSoundTypeCreate", "Catalog"))
                    .Read(read => read.Action("HeartSoundTypeRead", "Catalog"))
                    .Update(update => update.Action("HeartSoundTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("HeartSoundTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

