@(Html.Kendo().Grid<CardioSence.Models.Cadiomyopathy>()
              .Name("Cadiomyopathy")
              .Columns(columns =>
              {
                  columns.Bound(product => product.CadiomyopathyId).Width(70);
                  columns.Bound(product => product.CadiomyopathyName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.CadiomyopathyId);
                        model.Field(product => product.CadiomyopathyId).Editable(false);
                    })
                    .Create(create => create.Action("CadiomyopathyCreate", "Catalog"))
                    .Read(read => read.Action("CadiomyopathyRead", "Catalog"))
                    .Update(update => update.Action("CadiomyopathyUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("CadiomyopathyDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

