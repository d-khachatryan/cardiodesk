@(Html.Kendo().Grid<CardioSence.Models.TreatmentType>()
              .Name("TreatmentType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.TreatmentTypeId).Width(70);
                  columns.Bound(product => product.TreatmentTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.TreatmentTypeId);
                        model.Field(product => product.TreatmentTypeId).Editable(false);
                    })
                    .Create(create => create.Action("TreatmentTypeCreate", "Catalog"))
                    .Read(read => read.Action("TreatmentTypeRead", "Catalog"))
                    .Update(update => update.Action("TreatmentTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("TreatmentTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

