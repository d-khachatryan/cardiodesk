@(Html.Kendo().Grid<CardioSence.Models.Laun>()
              .Name("Laun")
              .Columns(columns =>
              {
                  columns.Bound(product => product.LaunId).Width(70);
                  columns.Bound(product => product.LaunName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.LaunId);
                        model.Field(product => product.LaunId).Editable(false);
                    })
                    .Create(create => create.Action("LaunCreate", "Catalog"))
                    .Read(read => read.Action("LaunRead", "Catalog"))
                    .Update(update => update.Action("LaunUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("LaunDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

