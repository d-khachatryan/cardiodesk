@(Html.Kendo().Grid<CardioSence.Models.PulmonaryDisease>()
              .Name("PulmonaryDisease")
              .Columns(columns =>
              {
                  columns.Bound(product => product.PulmonaryDiseaseId).Width(70);
                  columns.Bound(product => product.PulmonaryDiseaseName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.PulmonaryDiseaseId);
                        model.Field(product => product.PulmonaryDiseaseId).Editable(false);
                    })
                    .Create(create => create.Action("PulmonaryDiseaseCreate", "Catalog"))
                    .Read(read => read.Action("PulmonaryDiseaseRead", "Catalog"))
                    .Update(update => update.Action("PulmonaryDiseaseUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("PulmonaryDiseaseDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

