@(Html.Kendo().Grid<CardioSence.Models.PeripherialStatus>()
              .Name("PeripherialStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.PeripherialStatusId).Width(70);
                  columns.Bound(product => product.PeripherialStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.PeripherialStatusId);
                        model.Field(product => product.PeripherialStatusId).Editable(false);
                    })
                    .Create(create => create.Action("PeripherialStatusCreate", "Catalog"))
                    .Read(read => read.Action("PeripherialStatusRead", "Catalog"))
                    .Update(update => update.Action("PeripherialStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("PeripherialStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

