@(Html.Kendo().Grid<CardioSence.Models.HeartMurmurType>()
              .Name("HeartMurmurType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.HeartMurmurTypeId).Width(70);
                  columns.Bound(product => product.HeartMurmurTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.HeartMurmurTypeId);
                        model.Field(product => product.HeartMurmurTypeId).Editable(false);
                    })
                    .Create(create => create.Action("HeartMurmurTypeCreate", "Catalog"))
                    .Read(read => read.Action("HeartMurmurTypeRead", "Catalog"))
                    .Update(update => update.Action("HeartMurmurTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("HeartMurmurTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

