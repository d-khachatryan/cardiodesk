@(Html.Kendo().Grid<CardioSence.Models.COPD>()
              .Name("COPD")
              .Columns(columns =>
              {
                  columns.Bound(product => product.COPDId).Width(70);
                  columns.Bound(product => product.COPDName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.COPDId);
                        model.Field(product => product.COPDId).Editable(false);
                    })
                    .Create(create => create.Action("COPDCreate", "Catalog"))
                    .Read(read => read.Action("COPDRead", "Catalog"))
                    .Update(update => update.Action("COPDUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("COPDDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

