@(Html.Kendo().Grid<CardioSence.Models.RheumatizmDuration>()
              .Name("RheumatizmDuration")
              .Columns(columns =>
              {
                  columns.Bound(product => product.RheumatizmDurationId).Width(70);
                  columns.Bound(product => product.RheumatizmDurationName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.RheumatizmDurationId);
                        model.Field(product => product.RheumatizmDurationId).Editable(false);
                    })
                    .Create(create => create.Action("RheumatizmDurationCreate", "Catalog"))
                    .Read(read => read.Action("RheumatizmDurationRead", "Catalog"))
                    .Update(update => update.Action("RheumatizmDurationUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("RheumatizmDurationDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

