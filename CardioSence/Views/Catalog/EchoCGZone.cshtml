@(Html.Kendo().Grid<CardioSence.Models.EchoCGZone>()
              .Name("EchoCGZone")
              .Columns(columns =>
              {
                  columns.Bound(product => product.EchoCGZoneId).Width(70);
                  columns.Bound(product => product.EchoCGZoneName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.EchoCGZoneId);
                        model.Field(product => product.EchoCGZoneId).Editable(false);
                    })
                    .Create(create => create.Action("EchoCGZoneCreate", "Catalog"))
                    .Read(read => read.Action("EchoCGZoneRead", "Catalog"))
                    .Update(update => update.Action("EchoCGZoneUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("EchoCGZoneDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

