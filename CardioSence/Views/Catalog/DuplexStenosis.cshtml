@(Html.Kendo().Grid<CardioSence.Models.DuplexStenosis>()
              .Name("BicarbonateType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DuplexStenosisId).Width(70);
                  columns.Bound(product => product.DuplexStenosisName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DuplexStenosisId);
                        model.Field(product => product.DuplexStenosisId).Editable(false);
                    })
                    .Create(create => create.Action("DuplexStenosisCreate", "Catalog"))
                    .Read(read => read.Action("DuplexStenosisRead", "Catalog"))
                    .Update(update => update.Action("DuplexStenosisUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DuplexStenosisDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

