@(Html.Kendo().Grid<CardioSence.Models.DeathCause>()
              .Name("DeathCause")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DeathCauseId).Width(70);
                  columns.Bound(product => product.DeathCauseName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DeathCauseId);
                        model.Field(product => product.DeathCauseId).Editable(false);
                    })
                    .Create(create => create.Action("DeathCauseCreate", "Catalog"))
                    .Read(read => read.Action("DeathCauseRead", "Catalog"))
                    .Update(update => update.Action("DeathCauseUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DeathCauseDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

