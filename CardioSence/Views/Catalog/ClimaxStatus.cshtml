@(Html.Kendo().Grid<CardioSence.Models.ClimaxStatus>()
              .Name("ClimaxStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ClimaxStatusId).Width(70);
                  columns.Bound(product => product.ClimaxStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ClimaxStatusId);
                        model.Field(product => product.ClimaxStatusId).Editable(false);
                    })
                    .Create(create => create.Action("ClimaxStatusCreate", "Catalog"))
                    .Read(read => read.Action("ClimaxStatusRead", "Catalog"))
                    .Update(update => update.Action("ClimaxStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ClimaxStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

