@(Html.Kendo().Grid<CardioSence.Models.UrogenitalicDisease>()
              .Name("UrogenitalicDisease")
              .Columns(columns =>
              {
                  columns.Bound(product => product.UrogenitalicDiseaseId).Width(70);
                  columns.Bound(product => product.UrogenitalicDiseaseName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.UrogenitalicDiseaseId);
                        model.Field(product => product.UrogenitalicDiseaseId).Editable(false);
                    })
                    .Create(create => create.Action("UrogenitalicDiseaseCreate", "Catalog"))
                    .Read(read => read.Action("UrogenitalicDiseaseRead", "Catalog"))
                    .Update(update => update.Action("UrogenitalicDiseaseUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("UrogenitalicDiseaseDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

