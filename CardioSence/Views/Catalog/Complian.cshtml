@(Html.Kendo().Grid<CardioSence.Models.Complian>()
              .Name("Complian")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ComplianId).Width(70);
                  columns.Bound(product => product.ComplianName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ComplianId);
                        model.Field(product => product.ComplianId).Editable(false);
                    })
                    .Create(create => create.Action("ComplianCreate", "Catalog"))
                    .Read(read => read.Action("ComplianRead", "Catalog"))
                    .Update(update => update.Action("ComplianUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ComplianDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

