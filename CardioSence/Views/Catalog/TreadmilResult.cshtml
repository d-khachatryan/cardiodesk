@(Html.Kendo().Grid<CardioSence.Models.TreadmilResult>()
              .Name("TreadmilResult")
              .Columns(columns =>
              {
                  columns.Bound(product => product.TreadmilResultId).Width(70);
                  columns.Bound(product => product.TreadmilResultName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.TreadmilResultId);
                        model.Field(product => product.TreadmilResultId).Editable(false);
                    })
                    .Create(create => create.Action("TreadmilResultCreate", "Catalog"))
                    .Read(read => read.Action("TreadmilResultRead", "Catalog"))
                    .Update(update => update.Action("TreadmilResultUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("TreadmilResultDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

