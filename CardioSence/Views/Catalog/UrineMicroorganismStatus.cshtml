@(Html.Kendo().Grid<CardioSence.Models.UrineMicroorganismStatus>()
              .Name("UrineMicroorganismStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.UrineMicroorganismStatusId).Width(70);
                  columns.Bound(product => product.UrineMicroorganismStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.UrineMicroorganismStatusId);
                        model.Field(product => product.UrineMicroorganismStatusId).Editable(false);
                    })
                    .Create(create => create.Action("UrineMicroorganismStatusCreate", "Catalog"))
                    .Read(read => read.Action("UrineMicroorganismStatusRead", "Catalog"))
                    .Update(update => update.Action("UrineMicroorganismStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("UrineMicroorganismStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

