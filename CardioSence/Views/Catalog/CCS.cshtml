@(Html.Kendo().Grid<CardioSence.Models.CCS>()
              .Name("CCS")
              .Columns(columns =>
              {
                  columns.Bound(product => product.CCSId).Width(70);
                  columns.Bound(product => product.CCSName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.CCSId);
                        model.Field(product => product.CCSId).Editable(false);
                    })
                    .Create(create => create.Action("CCSCreate", "Catalog"))
                    .Read(read => read.Action("CCSRead", "Catalog"))
                    .Update(update => update.Action("CCSUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("CCSDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

