@(Html.Kendo().Grid<CardioSence.Models.InfectiousEndocarditis>()
              .Name("InfectiousEndocarditis")
              .Columns(columns =>
              {
                  columns.Bound(product => product.InfectiousEndocarditisId).Width(70);
                  columns.Bound(product => product.InfectiousEndocarditisName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.InfectiousEndocarditisId);
                        model.Field(product => product.InfectiousEndocarditisId).Editable(false);
                    })
                    .Create(create => create.Action("InfectiousEndocarditisCreate", "Catalog"))
                    .Read(read => read.Action("InfectiousEndocarditisRead", "Catalog"))
                    .Update(update => update.Action("InfectiousEndocarditisUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("InfectiousEndocarditisDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

