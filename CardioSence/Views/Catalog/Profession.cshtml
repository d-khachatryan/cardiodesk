@(Html.Kendo().Grid<CardioSence.Models.Profession>()
              .Name("Profession")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ProfessionId).Width(70);
                  columns.Bound(product => product.ProfessionName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ProfessionId);
                        model.Field(product => product.ProfessionId).Editable(false);
                    })
                    .Create(create => create.Action("ProfessionCreate", "Catalog"))
                    .Read(read => read.Action("ProfessionRead", "Catalog"))
                    .Update(update => update.Action("ProfessionUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ProfessionDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

