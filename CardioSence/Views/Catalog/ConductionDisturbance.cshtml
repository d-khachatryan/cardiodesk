@(Html.Kendo().Grid<CardioSence.Models.ConductionDisturbance>()
              .Name("ConductionDisturbance")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ConductionDisturbanceId).Width(70);
                  columns.Bound(product => product.LauConductionDisturbanceNamenName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ConductionDisturbanceId);
                        model.Field(product => product.ConductionDisturbanceId).Editable(false);
                    })
                    .Create(create => create.Action("ConductionDisturbanceCreate", "Catalog"))
                    .Read(read => read.Action("ConductionDisturbanceRead", "Catalog"))
                    .Update(update => update.Action("ConductionDisturbanceUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ConductionDisturbanceDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

