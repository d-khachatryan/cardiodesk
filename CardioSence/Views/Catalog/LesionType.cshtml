@(Html.Kendo().Grid<CardioSence.Models.LesionType>()
              .Name("LesionType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.LesionTypeId).Width(70);
                  columns.Bound(product => product.LesionTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.LesionTypeId);
                        model.Field(product => product.LesionTypeId).Editable(false);
                    })
                    .Create(create => create.Action("LesionTypeCreate", "Catalog"))
                    .Read(read => read.Action("LesionTypeRead", "Catalog"))
                    .Update(update => update.Action("LesionTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("LesionTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

