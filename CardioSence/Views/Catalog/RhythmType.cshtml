@(Html.Kendo().Grid<CardioSence.Models.RhythmType>()
              .Name("RhythmType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.RhythmTypeId).Width(70);
                  columns.Bound(product => product.RhythmTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.RhythmTypeId);
                        model.Field(product => product.RhythmTypeId).Editable(false);
                    })
                    .Create(create => create.Action("RhythmTypeCreate", "Catalog"))
                    .Read(read => read.Action("RhythmTypeRead", "Catalog"))
                    .Update(update => update.Action("RhythmTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("RhythmTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

