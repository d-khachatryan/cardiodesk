@(Html.Kendo().Grid<CardioSence.Models.CaroticBruit>()
              .Name("CaroticBruit")
              .Columns(columns =>
              {
                  columns.Bound(product => product.CaroticBruitId).Width(70);
                  columns.Bound(product => product.CaroticBruitName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.CaroticBruitId);
                        model.Field(product => product.CaroticBruitId).Editable(false);
                    })
                    .Create(create => create.Action("CaroticBruitCreate", "Catalog"))
                    .Read(read => read.Action("CaroticBruitRead", "Catalog"))
                    .Update(update => update.Action("CaroticBruitUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("CaroticBruitDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

