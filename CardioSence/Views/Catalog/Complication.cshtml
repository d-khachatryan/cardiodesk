@(Html.Kendo().Grid<CardioSence.Models.Complication>()
              .Name("Complication")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ComplicationId).Width(70);
                  columns.Bound(product => product.ComplicationName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ComplicationId);
                        model.Field(product => product.ComplicationId).Editable(false);
                    })
                    .Create(create => create.Action("ComplicationCreate", "Catalog"))
                    .Read(read => read.Action("ComplicationRead", "Catalog"))
                    .Update(update => update.Action("ComplicationUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ComplicationDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

