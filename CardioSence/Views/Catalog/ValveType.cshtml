@(Html.Kendo().Grid<CardioSence.Models.ValveType>()
              .Name("ValveType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ValveTypeId).Width(70);
                  columns.Bound(product => product.ValveTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ValveTypeId);
                        model.Field(product => product.ValveTypeId).Editable(false);
                    })
                    .Create(create => create.Action("ValveTypeCreate", "Catalog"))
                    .Read(read => read.Action("ValveTypeRead", "Catalog"))
                    .Update(update => update.Action("ValveTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ValveTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

