@(Html.Kendo().Grid<CardioSence.Models.DrugElutionType>()
              .Name("DrugElutionType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DrugElutionTypeId).Width(70);
                  columns.Bound(product => product.DrugElutionTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DrugElutionTypeId);
                        model.Field(product => product.DrugElutionTypeId).Editable(false);
                    })
                    .Create(create => create.Action("DrugElutionTypeCreate", "Catalog"))
                    .Read(read => read.Action("DrugElutionTypeRead", "Catalog"))
                    .Update(update => update.Action("DrugElutionTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DrugElutionTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

