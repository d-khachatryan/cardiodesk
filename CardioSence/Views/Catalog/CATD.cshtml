@(Html.Kendo().Grid<CardioSence.Models.CATD>()
              .Name("CATD")
              .Columns(columns =>
              {
                  columns.Bound(product => product.CATDId).Width(70);
                  columns.Bound(product => product.CATDName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.CATDId);
                        model.Field(product => product.CATDId).Editable(false);
                    })
                    .Create(create => create.Action("CATDCreate", "Catalog"))
                    .Read(read => read.Action("CATDRead", "Catalog"))
                    .Update(update => update.Action("CATDUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("CATDDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

