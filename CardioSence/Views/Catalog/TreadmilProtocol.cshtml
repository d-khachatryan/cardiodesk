@(Html.Kendo().Grid<CardioSence.Models.TreadmilProtocol>()
              .Name("TreadmilProtocol")
              .Columns(columns =>
              {
                  columns.Bound(product => product.TreadmilProtocolId).Width(70);
                  columns.Bound(product => product.TreadmilProtocolName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.TreadmilProtocolId);
                        model.Field(product => product.TreadmilProtocolId).Editable(false);
                    })
                    .Create(create => create.Action("TreadmilProtocolCreate", "Catalog"))
                    .Read(read => read.Action("TreadmilProtocolRead", "Catalog"))
                    .Update(update => update.Action("TreadmilProtocolUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("TreadmilProtocolDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

