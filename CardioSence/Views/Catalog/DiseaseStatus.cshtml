@(Html.Kendo().Grid<CardioSence.Models.DiseaseStatus>()
              .Name("DiseaseStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DiseaseStatusId).Width(70);
                  columns.Bound(product => product.DiseaseStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DiseaseStatusId);
                        model.Field(product => product.DiseaseStatusId).Editable(false);
                    })
                    .Create(create => create.Action("DiseaseStatusCreate", "Catalog"))
                    .Read(read => read.Action("DiseaseStatusRead", "Catalog"))
                    .Update(update => update.Action("DiseaseStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DiseaseStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

