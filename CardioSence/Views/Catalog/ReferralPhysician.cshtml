@(Html.Kendo().Grid<CardioSence.Models.ReferralPhysician>()
              .Name("ReferralPhysician")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ReferralPhysicianId).Width(70);
                  columns.Bound(product => product.ReferralPhysicianName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ReferralPhysicianId);
                        model.Field(product => product.ReferralPhysicianId).Editable(false);
                    })
                    .Create(create => create.Action("ReferralPhysicianCreate", "Catalog"))
                    .Read(read => read.Action("ReferralPhysicianRead", "Catalog"))
                    .Update(update => update.Action("ReferralPhysicianUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ReferralPhysicianDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

