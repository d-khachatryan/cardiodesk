@(Html.Kendo().Grid<CardioSence.Models.Insufficiency>()
              .Name("Insufficiency")
              .Columns(columns =>
              {
                  columns.Bound(product => product.InsufficiencyId).Width(70);
                  columns.Bound(product => product.InsufficiencyName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.InsufficiencyId);
                        model.Field(product => product.InsufficiencyId).Editable(false);
                    })
                    .Create(create => create.Action("InsufficiencyCreate", "Catalog"))
                    .Read(read => read.Action("InsufficiencyRead", "Catalog"))
                    .Update(update => update.Action("InsufficiencyUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("InsufficiencyDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

