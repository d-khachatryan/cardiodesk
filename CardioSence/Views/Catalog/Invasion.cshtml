@(Html.Kendo().Grid<CardioSence.Models.Invasion>()
              .Name("Invasion")
              .Columns(columns =>
              {
                  columns.Bound(product => product.InvasionId).Width(70);
                  columns.Bound(product => product.InvasionName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.InvasionId);
                        model.Field(product => product.InvasionId).Editable(false);
                    })
                    .Create(create => create.Action("InvasionCreate", "Catalog"))
                    .Read(read => read.Action("InvasionRead", "Catalog"))
                    .Update(update => update.Action("InvasionUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("InvasionDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

