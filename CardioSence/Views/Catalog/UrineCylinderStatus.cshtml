@(Html.Kendo().Grid<CardioSence.Models.UrineCylinderStatus>()
              .Name("UrineCylinderStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.UrineCylinderStatusId).Width(70);
                  columns.Bound(product => product.UrineCylinderStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.UrineCylinderStatusId);
                        model.Field(product => product.UrineCylinderStatusId).Editable(false);
                    })
                    .Create(create => create.Action("UrineCylinderStatusCreate", "Catalog"))
                    .Read(read => read.Action("UrineCylinderStatusRead", "Catalog"))
                    .Update(update => update.Action("UrineCylinderStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("UrineCylinderStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

