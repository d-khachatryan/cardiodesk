@(Html.Kendo().Grid<CardioSence.Models.CardioplegiaType>()
              .Name("CardioplegiaType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.CardioplegiaTypeId).Width(70);
                  columns.Bound(product => product.CardioplegiaTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.CardioplegiaTypeId);
                        model.Field(product => product.CardioplegiaTypeId).Editable(false);
                    })
                    .Create(create => create.Action("CardioplegiaTypeCreate", "Catalog"))
                    .Read(read => read.Action("CardioplegiaTypeRead", "Catalog"))
                    .Update(update => update.Action("CardioplegiaTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("CardioplegiaTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

