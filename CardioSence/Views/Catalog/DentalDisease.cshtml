@(Html.Kendo().Grid<CardioSence.Models.DentalDisease>()
              .Name("DentalDisease")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DentalDiseaseId).Width(70);
                  columns.Bound(product => product.DentalDiseaseName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DentalDiseaseId);
                        model.Field(product => product.DentalDiseaseId).Editable(false);
                    })
                    .Create(create => create.Action("DentalDiseaseCreate", "Catalog"))
                    .Read(read => read.Action("DentalDiseaseRead", "Catalog"))
                    .Update(update => update.Action("DentalDiseaseUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DentalDiseaseDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

