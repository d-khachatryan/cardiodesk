@(Html.Kendo().Grid<CardioSence.Models.Liquid>()
              .Name("Liquid")
              .Columns(columns =>
              {
                  columns.Bound(product => product.LiquidId).Width(70);
                  columns.Bound(product => product.LiquidName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.LiquidId);
                        model.Field(product => product.LiquidId).Editable(false);
                    })
                    .Create(create => create.Action("LiquidCreate", "Catalog"))
                    .Read(read => read.Action("LiquidRead", "Catalog"))
                    .Update(update => update.Action("LiquidUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("LiquidDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

