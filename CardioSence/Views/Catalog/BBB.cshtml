@(Html.Kendo().Grid<CardioSence.Models.BBB>()
              .Name("BBB")
              .Columns(columns =>
              {
                  columns.Bound(product => product.BBBId).Width(70);
                  columns.Bound(product => product.BBBName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.BBBId);
                        model.Field(product => product.BBBId).Editable(false);
                    })
                    .Create(create => create.Action("BBBCreate", "Catalog"))
                    .Read(read => read.Action("BBBRead", "Catalog"))
                    .Update(update => update.Action("BBBUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("BBBDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

