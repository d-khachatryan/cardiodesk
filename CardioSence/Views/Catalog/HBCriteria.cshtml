@(Html.Kendo().Grid<CardioSence.Models.HBCriteria>()
              .Name("HBCriteria")
              .Columns(columns =>
              {
                  columns.Bound(product => product.HBCriteriaId).Width(70);
                  columns.Bound(product => product.HBCriteriaName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.HBCriteriaId);
                        model.Field(product => product.HBCriteriaId).Editable(false);
                    })
                    .Create(create => create.Action("HBCriteriaCreate", "Catalog"))
                    .Read(read => read.Action("HBCriteriaRead", "Catalog"))
                    .Update(update => update.Action("HBCriteriaUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("HBCriteriaDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

