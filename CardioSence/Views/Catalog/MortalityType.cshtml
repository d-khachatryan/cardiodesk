@(Html.Kendo().Grid<CardioSence.Models.MortalityType>()
              .Name("MortalityType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.MortalityTypeId).Width(70);
                  columns.Bound(product => product.MortalityTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.MortalityTypeId);
                        model.Field(product => product.MortalityTypeId).Editable(false);
                    })
                    .Create(create => create.Action("MortalityTypeCreate", "Catalog"))
                    .Read(read => read.Action("MortalityTypeRead", "Catalog"))
                    .Update(update => update.Action("MortalityTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("MortalityTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

