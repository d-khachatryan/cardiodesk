@(Html.Kendo().Grid<CardioSence.Models.RelativeStatus>()
              .Name("RelativeStatus")
              .Columns(columns =>
              {
                  columns.Bound(product => product.RelativeStatusId).Width(70);
                  columns.Bound(product => product.RelativeStatusName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.RelativeStatusId);
                        model.Field(product => product.RelativeStatusId).Editable(false);
                    })
                    .Create(create => create.Action("RelativeStatusCreate", "Catalog"))
                    .Read(read => read.Action("RelativeStatusRead", "Catalog"))
                    .Update(update => update.Action("RelativeStatusUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("RelativeStatusDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

