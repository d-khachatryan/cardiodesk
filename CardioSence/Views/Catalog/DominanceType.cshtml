@(Html.Kendo().Grid<CardioSence.Models.DominanceType>()
              .Name("DominanceType")
              .Columns(columns =>
              {
                  columns.Bound(product => product.DominanceTypeId).Width(70);
                  columns.Bound(product => product.DominanceTypeName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.DominanceTypeId);
                        model.Field(product => product.DominanceTypeId).Editable(false);
                    })
                    .Create(create => create.Action("DominanceTypeCreate", "Catalog"))
                    .Read(read => read.Action("DominanceTypeRead", "Catalog"))
                    .Update(update => update.Action("DominanceTypeUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("DominanceTypeDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

