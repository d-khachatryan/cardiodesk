@(Html.Kendo().Grid<CardioSence.Models.RhFactor>()
              .Name("RhFactor")
              .Columns(columns =>
              {
                  columns.Bound(product => product.RhFactorId).Width(70);
                  columns.Bound(product => product.RhFactorName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.RhFactorId);
                        model.Field(product => product.RhFactorId).Editable(false);
                    })
                    .Create(create => create.Action("RhFactorCreate", "Catalog"))
                    .Read(read => read.Action("RhFactorRead", "Catalog"))
                    .Update(update => update.Action("RhFactorUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("RhFactorDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

