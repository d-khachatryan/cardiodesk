@(Html.Kendo().Grid<CardioSence.Models.Contractility>()
              .Name("Contractility")
              .Columns(columns =>
              {
                  columns.Bound(product => product.ContractilityId).Width(70);
                  columns.Bound(product => product.ContractilityName);
                  columns.Command(commands =>
                  {
                      commands.Destroy().Text("Delete").HtmlAttributes(new { @class = "Cat_Delete_Icon" });
                  }).Title("Commands").Width(110);
              })
              .ToolBar(toolbar =>
              {
                  toolbar.Create().Text("New");
                  toolbar.Save().SaveText("Save").CancelText("Cancel");
              })
              .Editable(editable => editable.Mode(GridEditMode.InCell))
              .DataSource(dataSource =>
                  dataSource.Ajax()
                    .Batch(true)
                    .Model(model =>
                    {
                        model.Id(product => product.ContractilityId);
                        model.Field(product => product.ContractilityId).Editable(false);
                    })
                    .Create(create => create.Action("ContractilityCreate", "Catalog"))
                    .Read(read => read.Action("ContractilityRead", "Catalog"))
                    .Update(update => update.Action("ContractilityUpdate", "Catalog"))
                    .Destroy(destroy => destroy.Action("ContractilityDestroy", "Catalog"))
              )
              .Pageable()
              .HtmlAttributes(new { @class = "catalog-grid" })
              .Scrollable()
)

